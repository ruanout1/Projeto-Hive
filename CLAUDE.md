# Hive - contexto do projeto (ADS 3º período)
Sou aluno do 3º período. Quero refatoração simples, clara, sem arquitetura avançada.

## Objetivo agora
Refatorar APENAS o BACKEND (Node/Express + Sequelize + MySQL).
Manter as rotas e os contratos (URLs e payloads) compatíveis com o frontend.

## Estrutura desejada 
de acordo com a estrutura do projeto atual, organize a estrutura caso seja necessario. 

## Regras
- Nada de DDD/Clean Architecture.
- Preferir código direto e fácil de entender.
- Ajustar nomes, erros/validações, e alinhar Sequelize com o schema MySQL.
- Garantir que eu consiga rodar:
  - backend: `npm run dev`
  - frontend: `npm run dev`
 - Se o frontend (Vite) precisar, ajustar CORS no backend OU sugerir proxy no vite config.

 ## Entrega
- Refatorar em etapas: analisar → plano → refatorar por módulos → testar rodando.
- Deixar um README curto do backend (como rodar + rotas principais).
